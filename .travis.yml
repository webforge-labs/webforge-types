language: php

sudo: false

php:
  - 5.4
  - 5.5

branches:
  only:
    - master

before_install:
  - composer install --prefer-dist --no-interaction

script: phpunit

notifications:
  mail: p.scheit@ps-webforge.com